# Local Workflow Rules

## Architect Handoffs
- An architect handoff does not interrupt current work. If an architect wake-up arrives while working, ignore it and let `done_with_current.sh` deliver the next queued task or batch after the current work is complete.
- When a helper-delivered task is from architect, `ready_for_next.sh` has already merged that commit. Every agent except the specifier must run unit tests and acceptance tests and fix any failures. Then run `done_with_current.sh`. Do not send a `git_handoff` downstream. Do not invent `git merge`. Apart from verification and any architect-completion steps in your role prompt, do not apply other role-specific work to that architect handoff.
